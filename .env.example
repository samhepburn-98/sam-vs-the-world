# Supabase — values from the project dashboard (Settings → API).
# The publishable key is safe to expose client-side; RLS gates all writes.
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
